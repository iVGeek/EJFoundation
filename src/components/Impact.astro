---
const stats = [
  { prefix: '', value: 1000, suffix: '+', label: 'People reached' },
  { prefix: '$', value: 10, suffix: 'K', label: 'To launch the helpline' },
  { value: 50, suffix: '+', label: 'Founding volunteers' },
  { value: 5, suffix: '+', label: 'Programs launching' }
];
---
<section id="impact" class="impact">
  <div class="container impact__head">
    <span class="section-eyebrow reveal">Year One</span>
    <h2 class="section-title reveal">New foundation, <em>big promise</em></h2>
    <p class="section-sub reveal">We're just getting started — and we're honest about it. These are the goals we're building toward in our first year, and we can't reach them without you.</p>
  </div>

  <div class="trust">
    <div class="trust__halo" aria-hidden="true"></div>
    <div class="container trust__grid">
      {stats.map((s,i)=>(
        <div class={`trust__item reveal reveal-d${(i%4)+1}`}>
          <div class="trust__num">
            {s.prefix && <span class="trust__num-prefix">{s.prefix}</span>}
            <span data-target={s.value} data-suffix={s.suffix}>0{s.suffix}</span>
          </div>
          <div class="trust__label">{s.label}</div>
        </div>
      ))}
    </div>
    <div class="trust__tagline">First year · Big promise · We need you</div>
  </div>
</section>

<style>
  .impact{padding:clamp(3.5rem,8vw,6rem) 0 0;}
  .impact__head{text-align:center;margin-bottom:clamp(2rem,5vw,3rem);}
  .impact__head .section-eyebrow{margin-inline:auto;}

  .trust{background:var(--grad-deep);position:relative;padding:clamp(3rem,7vw,4.5rem) 0 3rem;overflow:hidden;}
  .trust::before{content:'';position:absolute;right:-120px;top:-120px;width:360px;height:360px;background:radial-gradient(circle,rgba(201,106,60,.4),transparent 70%);border-radius:50%;}
  .trust::after{content:'';position:absolute;left:-80px;bottom:-140px;width:300px;height:300px;background:radial-gradient(circle,rgba(240,180,138,.15),transparent 70%);border-radius:50%;}
  .trust__halo{position:absolute;top:0;left:0;right:0;height:3px;background:var(--grad-brand);opacity:.9;}

  .trust__grid{position:relative;z-index:2;display:grid;grid-template-columns:repeat(4,1fr);gap:2rem;text-align:center;}
  .trust__num{font-family:var(--font-display);font-size:clamp(2.6rem,5vw,3.6rem);line-height:1;color:var(--on-dark);}
  .trust__num-prefix{font-size:.55em;margin-right:2px;opacity:.85;}
  .trust__num::after{content:'';display:block;width:34px;height:3px;background:var(--terra);border-radius:2px;margin:.9rem auto 0;transition:width .4s var(--ease-out);}
  .trust__item:hover .trust__num::after{width:52px;}
  .trust__label{font-size:.8rem;letter-spacing:.14em;text-transform:uppercase;color:rgba(250,244,234,.7);margin-top:.7rem;font-weight:600;}

  .trust__tagline{
    position:relative;z-index:2;text-align:center;
    margin-top:clamp(2rem,4vw,2.8rem);
    font-family:var(--font-display);font-style:italic;
    font-size:clamp(1rem,2vw,1.15rem);color:rgba(250,244,234,.65);
  }

  @media(max-width:800px){.trust__grid{grid-template-columns:1fr 1fr;gap:2.5rem 1rem;}}
  @media(max-width:560px){.trust__tagline{display:none;}}
</style>
